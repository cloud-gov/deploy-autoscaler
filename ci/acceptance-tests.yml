---
platform: linux

image_resource:
  type: registry-image
  source:
    aws_access_key_id: ((ecr_aws_key))
    aws_secret_access_key: ((ecr_aws_secret))
    repository: general-task
    aws_region: us-gov-west-1
    tag: ((harden-concourse-task-tag))   #/concourse/main/harden-concourse-task-tag has 1.0.33, latest in ECR is 1.0.62

inputs:
- name: autoscaler-manifests-test    #TODO: switch this back to autoscaler-manifests
outputs:
- name: someoutput

run:
  path: autoscaler-manifests-test/ci/acceptance-tests.sh #TODO: switch this back to autoscaler-manifests
